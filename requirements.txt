# ─────────────────────────────────────────────────────────────
# AgriNegotiator Requirements
# ─────────────────────────────────────────────────────────────

# ── Web Framework ──
fastapi==0.104.1
uvicorn[standard]==0.24.0
starlette==0.27.0

# ── Pydantic / Settings ──
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0.post1
python-dotenv==1.0.0

# ── Database ──
sqlalchemy==2.0.23
asyncpg==0.29.0
aiosqlite==0.22.1
alembic==1.18.5
psycopg2-binary==2.9.9

# ── Authentication & Security ──
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
bcrypt==4.0.1

# ── HTTP / Networking ──
requests==2.31.0
httpx==0.26.0
aiohttp==3.9.1

# ── Redis ──
redis==5.0.1

# ── AI / LangChain / LangGraph ──
langchain
langchain-core
langchain-community
langgraph
langchain-google-genai

# ── Embeddings / Vector DB ──
chromadb
sentence-transformers

# ── OpenAI compat (Featherless / Ollama) ──
openai>=1.0.0

# ── Monitoring ──
prometheus-client==0.19.0
prometheus-fastapi-instrumentator==6.1.0

# ── Utilities ──
python-multipart==0.0.6
tenacity==8.2.3
